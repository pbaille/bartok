; (ns bartok.melody.diatonic-passing
;   (:use [clojure.math.combinatorics :as c])
;   (:use bartok.primitives)
;   (:use utils.all))

; (b-fn degree-d-passing-context
;   "return the passing environment of a degree"    
;   [wm deg]
;   (let [up-dom ()
;         down-dom ()]
;     ))


; (b-multi d-passing-context) ;assign to each degree of the mode-class its potential passings role

; (b-meth d-passing-context 'WMode [wm] 
;   (entries->h-map (map (p degree-passing-tones wm) (:degrees mc))))

