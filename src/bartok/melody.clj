(ns bartok.melody
  (:use [bartok.litterals.identity])
  (:use [bartok.litterals.evaluation])
  (:use [utils.utils])
  (:use [bartok.types])
  (:use [bartok.melody.step-pattern]))
         
(load "melody/melodic_domain")      


